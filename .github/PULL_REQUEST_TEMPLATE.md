## What changes and why

<!-- The why matters more than the what: the diff already tells the what. -->

## Related issue

<!-- Anything beyond a small fix should have one. See CONTRIBUTING.md. -->

## Checked

- [ ] `npm run typecheck` · `npm run lint` · `npm test` · `npm run build`
- [ ] New UI copy is in `lib/i18n/dictionary.ts`, in both `en` and `es`
- [ ] No real CV or personal data in code, tests or fixtures
- [ ] Touches a prompt or a model-facing schema? Say which, so the eval runs before release
- [ ] Adds a model call? It records its spend

## If something visible changed

<!-- Screenshots, or how to look at it. Tests don't see layout. -->
