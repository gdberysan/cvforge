# Bindings

Dónde vive cada pieza de este proyecto. Los **IDs** son la identidad: renombrar un
equipo o un proyecto en Vercel no los cambia, así que esta tabla sobrevive a
cualquier renombre.

| Superficie | Valor |
|---|---|
| GitHub | `gdberysan/cvforge` |
| Vercel | `cvforge-app` · `prj_j2ULZd5neUIRsRP7asO5HTKb3H4x` · Root Directory `.` |
| Dominio | https://cvforge.korven.dev |
| Datos | SQLite local en la máquina de cada usuario — no hay base de datos alojada |
| Supabase | ninguno (a propósito: CVForge no guarda nada en servidores) |

Interno: este archivo está en `.gitattributes` como `export-ignore` — no viaja
en el zip de venta.
